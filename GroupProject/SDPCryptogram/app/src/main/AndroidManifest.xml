<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.seclass.sdpcryptogram">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminHomePage" />
        <activity android:name=".Player_Home_Page" />
        <activity android:name=".Add_Cryptograms" />
        <activity android:name=".Add_Player" />
        <activity android:name=".View_Prior" />
        <activity android:name=".Solve_Cryptogram" />
        <activity android:name=".Ratings" />
        <activity android:name=".Cryptogram_List" />
        <activity android:name=".Solve_Prior"></activity>
    </application>

</manifest>